import { IPageProps } from "@/types";

const HomePage = ({ device }: IPageProps) => {
    return <>
        <h1>Welcome to <span style={{ fontStyle: 'italic'}}>WhizDom</span> !</h1>
    </>
}

export default HomePage;
